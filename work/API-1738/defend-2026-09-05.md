# Defend the changes — API-1738 · PR #2484 · diff `origin/master` (e004bf9d8) … `1e65eb8a3` (2026-09-05)

Reviewed diff = merge-base(origin/master, branch)..branch = exactly 1 commit, 14 files, +214/−87. Semantic drift since the merge-base: **none** (the branch is rebased on the current master; 0 master commits unseen). Sources: ticket API-1738 (description Parts 1–4, matrix rows 1–12, VB answers 2026-09-02 #1–#6), PR #2484 reviews (nowakmarcin T01–T20), plan `phase-05-plan.md`, ApiLLM `origin/main` docs (anchor e004bf9d, CURRENT).

| # | File · change | Who asked / why (citable) | Why this approach | Side effects | Breaking? | Verdict |
|---|---|---|---|---|---|---|
| 1 | `Concepts/_Shared/Booking/ContactDataMaskingProvider.cs` (new) — `ShouldMaskContactData`: DisplayPassengers via `IManageAndCheckinRulesValidator`, exempt when `IsVerificationRequired` | Ticket Parts 1+3; reviewer T19 option 2 ("exactly like it was before with ContactDataMaskingProvider"), T12 (no default), T13 (no `[UsedImplicitly]`) | one decision point reused by three endpoints (ARC-14); alternatives B/C rejected in plan §Design | new singleton (registry:463); consumed only by `BookingOutputBuilder` | no | DEFENSIBLE |
| 2 | `Concepts/Booking/GetBooking/ModelBuilders/BookingOutputBuilder.cs` — injects the provider, computes `maskContactData` and `showChildCustomerNumber` once, passes booleans down; `User` built with the flag | Ticket Part 1 (username), T11 ("resolved in BookingOutputBuilder and passed downstream"), T19 | downstream boolean = reviewer-accepted round-1 shape | `ShowChildCustomerNumber` evaluation moved here from the passengers builder (same validator, same args) — behaviour identical | no | DEFENSIBLE |
| 3 | `BookingPassengerOutputBuilder.cs` — drops the rule validator and `MASKED_DATA`; identity fields in clear; only `Phone`/`EmailAddress` masked with the new formats | Ticket Part 1 + Background ("update the fields"); masking formats section | fields listed by the ticket; nothing else touched (PRC-97) | privacy surface reduced by request (PR checkbox explained) | values only | DEFENSIBLE |
| 4 | `BookingContactsOutputBuilder.cs` — `Build(booking, bool)`, email + 5 phone types masked via `GetPhoneNumber` | Ticket Part 1 ("Email & Phones … all kinds of contacts: Primary, Emergency") | one helper for the five phone types (ARC-87) | single caller | values only | DEFENSIBLE |
| 5 | `Concepts/_Shared/Models/Output/BookingOutput.cs` — `Create(agent, channel, bool)` (no default), `CreateFromBookingComment(booking, bool = false)`, `MaskUsernameForCustomerAccount` | Ticket Part 1 ("Creation Username: masked when the creator's account type is Customer"); T01 (handle inside the factories); abe4bb5d8 (Luis) dropped the `Create` default | factory keeps the decision next to the data | other `CreateFromBookingComment` callers (`BookingNotificationsProvider.cs:172`, `PaxDataCollectorFormNotification.cs:52`) unchanged thanks to the kept default — see WEAK note | no | DEFENSIBLE (kept default: WEAK per STY-05, justified; reviewer may prefer explicit `false` at 2 call sites) |
| 6 | `Concepts/Booking/Rules/Criteria/Configurable/CreationAccountCriteria.cs` — second bypass `TreatContactEmailMatchAsSameAccount && IsContactEmailTheSame` before `Verify` | Ticket Part 2 (OR condition, same evaluation point); T03 (name), T04 (no null check on `UserName`); VB #2 keeps the toggle name | mirrors the existing creator-match bypass line | shared by every rule's `creationAccount` block, gated by a per-rule toggle seeded false | no (default off) | DEFENSIBLE |
| 7 | `Configuration/Parts/BookingRulesConfigPart.cs` — `TreatContactEmailMatchAsSameAccount` | Ticket Part 2 | bool on `CreationAccountCriteriaConfig` next to `EnabledForTheSameAccount` | missing key in stored docs → false (ROB-78) | no | DEFENSIBLE |
| 8 | `Concepts/Admin/ConfigPart/Schema/BookingRulesConfigPartSchema.cs` — schema field + description | ARC-95 (schema 1:1), PRC-37; T02 asked to check the name → VB kept it | description text states the semantics for Admin-Portal users | Admin Portal shows the new toggle | no | DEFENSIBLE |
| 9 | `Assets/Seed/AdminPortal/SettingsDocuments/BookingRules.json` — `treatContactEmailMatchAsSameAccount: false` ×20 | PRC-37 (seed aligned with schema, sensible default) | every rule entry carries the block → all 20 updated (ARC-90) | fresh environments only (seeder skips existing docs) | no | DEFENSIBLE |
| 10 | `Infrastructure/Extensions/StringExtensions.cs` — `ToMaskedPhone` (+ constant) | Ticket "Phone → new format: last 4 digits visible, rest *" | next to `ToMaskedEmail`, reused by both booking builders (ARC-63) | none (new method) | no | DEFENSIBLE |
| 11 | `Infrastructure/Ioc/ConceptsSharedRegistry.cs` — one `ForSingletonOf<IContactDataMaskingProvider>()` line in the shared block | ARC-12; T14 (move to Shared), T20 (no other changes in the file) | explicit registration | — | no | DEFENSIBLE |
| 12 | `Concepts/Account/GetAccount/ModelBuilders/AccountTripOutputBuilder.cs` — email always masked + rationale comment | Ticket Part 4; VB #4 (both `upcomingTrip` and `basket.travelSummary`), VB #5 (always, no rule); T17 (comment approved) | one builder serves both account paths | `GET /v1/Account` email values masked | values only | DEFENSIBLE |
| 13 | `Concepts/AccountTrips/_Shared/ModelBuilders/TripOutputBuilder.cs` — `maskContactData` parameter removed, email always masked, comment | Ticket Part 4 (Trips + Trips/Add unconditional); VB #5; T18 (Trips/Add must mask, remove the parameter); T09/T10 moot | same builder for list and Add (ARC-87) | `GET /v1/Account/Trips`, `POST /v1/Account/Trips/Add` email masked | values only | DEFENSIBLE |
| 14 | `Concepts/AccountTrips/GetTrips/DotRezApi/RetrieveBookingsRequest.cs` — projects `contacts.value.distributionOption` | VB #6 (no per-PNR fan-out); T08; `GetContact` filters on `DistributionOption == Email` (`Integration/DotRezApi/Extensions/Booking.cs:43`) so the primary contact never resolved on the light retrieve | one scalar in the existing grouped query instead of N `GetBookingRequest` calls (ROB-80) | trips `email` was `null` before → masked value now (consumers) | behaviour change, stated in PR | DEFENSIBLE |

**Breaking-change checklist (this API):** output models — no field removed/renamed/typed differently; values newly masked (booking endpoints when the rule denies; account endpoints always) and one null→value change (trips email) — all requested, stated in the PR. Booking-rules criteria/schema — one new toggle, default false, seed aligned. DotRez projections — one field added, nothing removed. DI — one registration, ctor changes only on scan-registered builders with single callers. Endpoint behaviour — no path/status/error changes; no new fan-out (a fan-out was removed vs the previous revision). Error surface — unchanged (247 codes).

**Summary:** 14 entries, 14 DEFENSIBLE (entry 5 carries a WEAK note on the kept default, justified by two external callers). 0 UNJUSTIFIED, 0 RISKY.

**Open questions:** none blocking. Reviewer's call on entry 5 (explicit `false` at two notification call sites vs the default) — offered in the T12 reply.

**Drafts:** `pr-replies-draft.md` (T01–T20 + TL summary), `pr-description-draft.md`.
